# opencode-tianji 0.5.0 实施交付说明

> 实施时间:2026-08-08 · 依据《调研报告_开源占卜生态对比.md》P0-P2 全量待办

## 本次完成(0.4.0 → 0.5.0)

### P0 差异化变现
| 项 | 状态 | 证据 |
| --- | --- | --- |
| 卦例库扩充 26→375 例 | ✅ | `data/guaili.json`:增删卜易(书4)260 例 + 卜筮正宗(书6)115 例,含卦象/断语/应期原理/白话/来源溯源;`scripts/check_guaili.ts` 质检(字段/卦名/变卦/行号/去重) |
| 六爻解卦评测基准 | ✅ | `benchmark/`:guaili_gold.json(310 例)+ run.ts + report.md。**排盘可判**(引擎 232 例零失败)+ **断语可溯**(用神命中 213/232=91.8%);hex 新增 `buildPanByGanzhi`(干支直入排盘) |
| 古籍 PR 化 | 🟡 | README 首页「独家卖点」区已加;`docs/收录申请资料.md` 备好,待用户确认后提交 awesome-mcp-servers / mcp.so PR |

### P1 工程对齐
| 项 | 状态 | 证据 |
| --- | --- | --- |
| hex 与 lunar 对账 | ✅ | `test/duizhang.test.ts`:年/月/日干支 72 天抽样、节气月边界、立春时刻 ±2 分钟,全部一致 |
| 全工具可审计 | ✅ | `qigua`/`paipan` 支持 `format="json"` 输出结构化审计(口径/依据元数据) |
| 周易本义提取 | 🟡 | 书8 状态「未提取」→「部分」(说卦传取象已入 bagua.json);朱熹卦爻注留待后续 |

### P2 形态与增长
| 项 | 状态 | 证据 |
| --- | --- | --- |
| 农历/黄历补位 | ✅ | 新 `almanac` 工具(lunar-javascript,农历/干支/宜忌/吉神方位/冲煞/旬空),8 工具齐 |
| stdio MCP 通道 | ✅ | `mcp/index.ts`:8 工具全部注册且参数 schema 完整,进 Claude/Cursor/Codex |
| 独立引擎入口 | ✅ | `lib/engine.ts` + package.json `exports["./engine"]`,纯计算(hex+ganzhi,仅 astronomy-engine) |
| 解卦任务书 | ✅ | `duangua` 输出末尾「解卦任务书」段:用神/旺衰/空破已算好,解读方只做组织表达 |

## 测试
`bun test`:78 项全过(原 44 → 78,新增卦例/benchmark/对账/JSON审计/黄历)。181 expect。

## 依赖
- 运行时:`@opencode-ai/plugin` + `astronomy-engine` + `lunar-javascript`(均 MIT)
- 测试:`lunar-javascript`(兼作对账基准)

## 发布
```sh
cd opencode-tianji
npm run build && npm publish   # 0.5.0
```

## 补充完善(0.5.0 发布后)

| 项 | 状态 | 证据 |
| --- | --- | --- |
| 朱熹卦爻注提取 | ✅ | 书8 新增「卷注」:卷一·上经 30 卦 + 卷二·下经 34 卦,共 64 卦 / 386 爻注 / 彖曰 180 段 / 象曰 448 段 / 文言 48 段(乾坤),反切音注全保留;待提取仅剩卷三系辞/卷四序卦杂卦 |
| 卦例库干支补全 | ✅ | 33 条「干支未详」中 31 条据原文排盘表(爻1尾=月建干支、爻2尾=日辰)+ 断语交叉反推补全,并逐一经引擎验证(月破/旬空/月建生世与断语吻合);仅剩 2 条原文确无干支记录 |
| TypeScript 类型声明 | ✅ | `types/index.d.ts`(插件主入口)+ `types/engine.d.ts`(引擎纯计算层),package.json 补 `types` 字段与 exports 的 types 条件;tsc 严格模式类型检查 0 错误 |

> 测试:83 → **84 项全过**(新增朱熹卷注 5 项 + 卦例干支收敛 1 项)

## 0.5.2(古籍补全 + 收录)

| 项 | 状态 | 证据 |
| --- | --- | --- |
| 朱熹卷三/四补全 | ✅ | 书8 卷注新增「卷三·系辞传」(上/下传各 12 章)、「卷四·序卦传」(61 条)、「卷四·杂卦传」(34 条),系辞注 153 条;卷三不含独立文言传(文言注已在卷一乾36/卷二坤12);书8 提取状态「部分→完整」 |
| 卦例库扩充 | ✅ | 375 → **381 例**(书4 +3、书6 +3),两书已收录极全(全书级扫掠确认净可增 6 则);引擎逐条复核 干支/卦名/变卦 吻合 |
| 书1 十翼提取 | ✅ | 新 `data/十翼.json`:系辞上 33 段/系辞下 50 段/文言乾16坤6/序卦 64 条/杂卦 64 条,全部带行号溯源;序卦卦序与爻辞.json 逐条核对一致 |
| awesome-mcp 收录 | ✅ | PR #11717 已提交(punkpeye/awesome-mcp-servers,Spirituality & Esoterica 分区,首个六爻条目) |

> 测试:84 → **94 项全过**(新增朱熹卷三四 5 项 + 十翼 5 项)。镜像 28 文件全一致,`check_guaili.ts` 381 例全过。

## 待办(后续)
- [ ] mcp.so 提交(需注册账号;stdio MCP 已就绪,可直接提交仓库 URL)

## 0.5.3(八书全量独立模块 + 工具/命令完善)

| 项 | 状态 | 证据 |
| --- | --- | --- |
| 六爻独立模块 | ✅ | `modules/zhanbu.ts` 瘦身为纯聚合器;六爻 4 工具(qigua/paipan/duangua/cha)拆入 `modules/liuyao.ts`,自声明导出,不再耦合其他术别 |
| 大衍筮法 | ✅ | `plugins/dayan.ts`:蓍草四营十八变(分二/挂一/揲四/归奇),三变成爻、十八变成卦,seed 可复现;输出本卦/变卦/卦辞/动爻爻辞 |
| 焦氏易林占 | ✅ | `plugins/yilin.ts`:本卦+之卦取 4096 首变诗,之卦可指定/动爻推/随机(seed 复现),诗译白话+书7溯源 |
| 京氏易传占 | ✅ | `plugins/jingshi.ts`:八宫/宫五行/世应/纳甲六亲/飞伏神(消费 jingshi_fufu.json 64 卦),据占事取用神看显伏 |
| 火珠林 | ✅ | 新 `data/huozhulin.json`(59 门类,原文+白话+行号溯源,双镜像)+ `plugins/huozhulin.ts` 按占事匹配口占断辞 |
| 经传查注 | ✅ | `plugins/jingdian.ts` chazhu:朱熹《周易本义》卦爻注 + 十翼(序卦/杂卦/文言)+ 彖传大象,附白话导读 |
| cha/duangua 增强 | ✅ | `cha` 追加朱熹卦爻注 + 序卦/杂卦/文言;`duangua` 附「相似卦例」佐证(381 例卦例库,白话摘要+溯源) |
| 命令补位 | ✅ | 新增 6 快捷命令 `/paipan` `/meihua` `/bazi` `/liuren` `/almanac` `/cha`,与 `/zhanbu` 并存 |
| 技能补位 | ✅ | meihua/bazi/liuren 轻量 SKILL.md(最小信息先问后算 + 白话铁律) |
| 镜像同步 | ✅ | plugins/lib/templates 全部同步 `.opencode`;huozhulin 同步知识库;全部 diff 一致 |
| MCP | ✅ | 13 工具全注册(新增 dayan/yilin/jingshi/huozhulin/chazhu) |

> 测试:94 → **131 项全过**(新增 dayan/yilin/jingshi/huozhulin/jingdian/liuyao增强 等 16 文件)。镜像 diff 全一致。工具总数 **13**。

> 架构:八部典籍算法全部独立成模块(liuyao/meihua/dayan/yilin/jingshi/huozhulin/jingdian/bazi/liuren/almanac),`zhanbu.ts` 仅聚合注册,单向依赖 lib,术间零耦合。

## 0.5.4(目录迁移 + 一致性修复)

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 模块目录迁移 | ✅ | 10 个术模块从 `plugins/` 全部迁至 `modules/`(修复 opencode 加载器误判),`plugins/` 仅保留 `zhanbu.ts` 聚合器 |
| 繁体卦名归一 | ✅ | 原 yilin/liuyao/jingdian 三处重复的繁→简映射表统一为 `lib/hex.ts` 的 `simpGuaName`(易林/朱熹注/查卦共用) |
| 用神世应修复 | ✅ | paipan/duangua 对 世/应 用神不再输出"卦中无世爻",直取世应爻位 |
| 全称卦名 | ✅ | qigua manual/paipan/duangua/cha/meihua/huozhulin 统一支持 "天水讼"/"兑为泽" 等全称输入 |
| 金标准 id 去重 | ✅ | gold.ts id 追加序号,消除 16 组同源行号重复 |
| 输入防御 | ✅ | parseDT 校验非法日期(13月/2月30日/25时);xunKong 对非法日干支抛中文错误 |
| 评测统计修正 | ✅ | run.ts 区分「缺干支」与「引擎错误」;占事分类正则"会试"先于求财判定 |
| 测试修正 | ✅ | zhanbu.test.ts 恒真断言(?? true)改为真实字段断言;`原文/` 目录收录 8 部古籍 txt(人工参考,不入包) |

> 测试:107 项全过(镜像 6 项因新环境无 `知识库` 目录自动 skip,设置 `TIANJI_MIRROR` 可恢复)。

## 0.6.0(真实实例 E2E + 文档同步 + 开发宪法)

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 真实 opencode 实例 E2E 套件 | ✅ | 新 `e2e/`:隔离临时项目 + 真实 opencode serve/run。确定性验证 13 工具注册(serve `/experimental/tool/ids` + `x-opencode-directory`)+ 真实 LLM 对话调用天机工具(NDJSON `tool_use` completed 断言)+ `/zhanbu` 命令模板验证。入口 `bun run test:e2e` / `test:e2e:skip-llm`,汇总 `report.json`,全程 XDG 隔离、apiKey 零落盘 |
| E2E header 中文路径修复 | ✅ | `e2e/tools.ts` `x-opencode-directory` 改 `encodeURIComponent`(服务端 URL 解码契约),中文/非 ASCII 路径可用 |
| 开发宪法 | ✅ | 新增 `AGENTS.md`:规则分级(铁律/约定/建议)、架构铁律(modules/ 目录、单向依赖、数据溯源、历法对账)、**文档同步跟踪**(改动→文档映射表、提交前检查清单、镜像同步)、占卜输出铁律、验收门禁 |
| 文档同步 | ✅ | README 全量同步(13 工具、modules/ 布局、381 卦例、346 金标准、E2E 命令、历法/审计节);交付说明补 0.5.4 条目;package.json description 同步 381 卦例 |
| 数据/代码收尾 | ✅ | benchmark 金标准更新(346 例)、`lib/hex.ts`/`modules/*`/`mcp/index.ts`/`templates/*`/`test/*` 一致性修正 |

> 测试:107 pass / 0 fail(6 镜像项 skip);`bun test e2e/` 51 项单测全过;`bun run test:e2e:skip-llm` 13 工具全部注册(missing: [])。发布:0.5.4 → **0.6.0**。
